<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <EditText
            android:id="@+id/name_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            android:hint="@string/goal_name_hint"
            android:inputType="text"
            android:maxLines="1" />

        <NumberPicker
            android:id="@+id/goal_reminder_period"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/name_edit_text"
            app:layout_constraintLeft_toRightOf="@id/goal_period_hint"
            android:layout_marginLeft="16dp"
            android:orientation="horizontal"/>

        <TextView
            android:id="@+id/goal_period_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@id/goal_reminder_period"
            app:layout_constraintBottom_toBottomOf="@id/goal_reminder_period"
            app:layout_constraintLeft_toLeftOf="parent"
            android:textSize="17sp"
            android:text="@string/goal_period_hint" />

        <TextView
            android:id="@+id/goal_unit_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="17sp"
            android:text="@string/goal_period_unit"
            app:layout_constraintTop_toTopOf="@id/goal_reminder_period"
            app:layout_constraintBottom_toBottomOf="@id/goal_reminder_period"
            app:layout_constraintLeft_toRightOf="@id/goal_reminder_period"
            android:layout_marginLeft="16dp" />

        <Button
            android:id="@+id/goal_add_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/goal_reminder_period"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/blue"
            android:textAlignment="center"
            android:textAllCaps="true"
            android:padding="8dp"
            android:enabled="@{nameEditText.text.length() > 0}"
            android:text="@string/add_goal_button" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>